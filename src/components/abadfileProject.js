// // renamed: pre-pended "abadfile" to Project.js. I want to keep this in case i find the jsx helpful
// import CallToAction from "./CallToAction";

// function Project({slug, title, body, projectType, titleImage, titleImageAltText}) {
// 	if (projectType==='assignment') {
// 		return (
// 			<div className={`singleProject project-grid ${slug} ${projectType}`}>
// 				{/* TO DO: Replace class: project-grid with something that makes more sense
				
// 					It sould have (or be) a link to a page with more details about the
// 					project if that makes sense. 
// 					To indicate that it's clickable it could also surge on mobile (how?)/ have a tracer around it on hover... */}
// 				<h2 className="projectTitle">{title}</h2>
// 				<p className="projectBody">{body}</p>
// 				<img className="projectPicture" src={titleImage} alt={titleImageAltText} />
// 				<CallToAction buttonText={title} />
// 			</div>
// 		);
		
// 	}

// }
// export default Project;
